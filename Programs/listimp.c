#include<stdio.h>
struct node{
	int data;
	struct node* next;
};
int struct node* newnode(int value);
	struct node* temp;
	(struct node*)malloc(sizeof(struct node* temp);
	temp->data=data;
	temp->next=NULL;
}
