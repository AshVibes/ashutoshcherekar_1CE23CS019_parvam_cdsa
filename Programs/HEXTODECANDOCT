#include<stdio.h>
int main()
{
	unsigned int num;
	printf("Enter the hexadecimal number");
	scanf("%x",&num);
	printf("The decimal value is %d",num);
	printf("The octal value is %o",num);
	return 0;
}
